import { CommsHttpInterceptor } from './comms-http-interceptor';

describe('CommsHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new CommsHttpInterceptor()).toBeTruthy();
  });
});
